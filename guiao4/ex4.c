#include <unistd.h>

int main(int argc, char * argv[]) {

    //preparar associaçoes
    //remover o -i e o -o do argv e passa-los para o dup
    //dup2(argv[2],0);
    //dup2(argv[4],1);


    return 0;
}